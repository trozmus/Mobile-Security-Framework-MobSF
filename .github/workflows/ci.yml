name: CI

on:
  pull_request:

jobs:
  docker-build:
    name: Docker build check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Build Docker image
        run: docker build -t mobsfscan-ci:${{ github.sha }} .
